import { PagePlaceholder } from "@/frontend/pages/_shared/PagePlaceholder";

export function CalendarPage() {
  return (
    <PagePlaceholder
      title="Calendar"
      description="Draw calendar, next-draw countdown, month insights, and date-based historical signals."
    />
  );
}

import { PagePlaceholder } from "@/frontend/pages/_shared/PagePlaceholder";

export function BacktestPage() {
  return (
    <PagePlaceholder
      title="Backtest"
      description="Strategy comparison, historical ranges, hit rate, miss streaks, and performance charts."
    />
  );
}

import { PagePlaceholder } from "@/frontend/pages/_shared/PagePlaceholder";

export function WatchlistPage() {
  return (
    <PagePlaceholder
      title="Watchlist"
      description="Saved numbers, personal tags, notes, historical stats, and signal monitoring."
    />
  );
}

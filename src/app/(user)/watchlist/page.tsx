import { PagePlaceholder } from "@/frontend/pages/_shared/PagePlaceholder";

export default function WatchlistPage() {
  return (
    <PagePlaceholder
      description="Watchlist workspace is not wired into the current product surface yet."
      title="Watchlist"
    />
  );
}

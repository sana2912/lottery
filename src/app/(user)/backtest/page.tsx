import { PagePlaceholder } from "@/frontend/pages/_shared/PagePlaceholder";

export default function BacktestPage() {
  return (
    <PagePlaceholder
      description="Backtest workspace is not wired into the current product surface yet."
      title="Backtest"
    />
  );
}
